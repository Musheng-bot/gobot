# NavInterface

NavInterface的职责是和其他模块的通信，承载所有和ros2相关的职能，主要是`节点日志`，`话题通信`和`服务通信`三个部分。  
`NavInterface`一般只和`RushExtended`做交互，其他模块不和`NavInterface`直接交互

## 话题

NavInterface承载的话题通信如下

## 服务

NavInterface承载的服务通信如下
